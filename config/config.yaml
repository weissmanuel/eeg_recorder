

defaults:
  - headset: unicorn
  - _self_

signal_id: ${headset.source_id}
#marker_id: 'rsvp_markers'
marker_id: null

buffer_size_seconds: 60

preprocessors:
  - name: 'eeg_scaler'
    scale: 1e-6
    channel_types: ['eeg']