

defaults:
  - experiment: demo
  - headset: demo
  - persistence: persistence
  - psd: welch
  - real_time: default
  - recording: default
  - visualization: default
  - _self_

sources: ${experiment.sources}